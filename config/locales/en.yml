en:
  activerecord:
    attributes:
      event:
        slug: URL
        starts_on: Start date
        ends_on: End date
  errors:
    attributes:
      ends_on:
        invalid: can’t be before start date

  profile:
    updated: "Your profile was updated"

  events:
    form:
      create: "Create event"
      save: "Save settings"
    show:
      register: "Register now"
    edit:
      tabs:
        settings: Settings
        packages: Packages

  packages:
    new:
      default: "Basic"

  activity_types:
    new:
      default: "activity"

  registrations:
    new:
      heading: "Register for %{event}"
      continue: "Continue"
    select_package:
      heading: "Select package"
      continue: "Continue"
    thanks:
      heading: "Thanks!"
      body: "You have registered for %{event}."

  devise:
    sessions:
      new:
        login: "Log in"
